nelmio_api_doc:
    documentation:
        info:
            title: Secure Storage API
            description: This app stores your data securely.
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
            - ^/item(?)
            - /login
            - /logout
